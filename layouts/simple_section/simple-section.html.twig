{#
/**
 * @file
 * A simple layout intended to contain a hero. This layout does not have any
 * associated configuration options.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}
{% if content %}

  <section{{ attributes.addClass('layout--simple-section') }}>

    {% if content.first %}
      {{ content.first }}
    {% endif %}

  </section>
  
{% endif %}
